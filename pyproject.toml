[project]
name = "residalink"
version = "0.1.0"
description = "Plateforme de vie quotidienne pour copropriétés"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0,<7.0",
    "pillow>=10.0",
    "whitenoise>=6.6",
    "gunicorn>=22.0",
    "dj-database-url>=2.1",
    "psycopg[binary]>=3.1",
]
